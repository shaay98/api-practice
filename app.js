"use strict"

const url = "https://pokeapi.co/api/v2/pokemon/";

async function getData() {
    const response = await fetch (url);
    console.log(response)
    
let renderList = pokemon.name
    console.log(pokemon.name)
} 
getData();


